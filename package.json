{
  "name": "orbit-social-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Orbit social platform monorepo",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "seed": "pnpm --filter @orbit/api seed",
    "crud:smoke": "pnpm --filter @orbit/api crud:smoke"
  },
  "engines": {
    "node": ">=20"
  }
}
