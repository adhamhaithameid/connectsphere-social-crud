{
  "name": "connectsphere-social-crud",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack social media CRUD platform for internship portfolio",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "seed": "pnpm --filter @connectsphere/api seed",
    "crud:smoke": "pnpm --filter @connectsphere/api crud:smoke"
  },
  "engines": {
    "node": ">=20"
  }
}
